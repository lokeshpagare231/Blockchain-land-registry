# Blockchain Scripts

Utility scripts to inspect and monitor local blockchain state.

## Files

- `deployment.local.json`: generated by `smart-contracts/scripts/deploy.js`
- `watch-events.js`: listen to smart contract events
- `inspect-chain.js`: print recent block metadata and registration count

## Run

```bash
npm install
npm run inspect-chain
npm run watch-events
```
