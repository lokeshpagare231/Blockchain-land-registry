# Frontend Module

Next.js dashboard for blockchain simulation, explorer, registry, AI verification, and network monitoring.

## Run

```bash
npm install
npm run dev
```

Set API URLs in `.env` based on `.env.example`.
