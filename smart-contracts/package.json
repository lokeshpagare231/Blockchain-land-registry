{
  "name": "land-registry-smart-contracts",
  "version": "1.0.0",
  "private": true,
  "description": "Hardhat project for blockchain-based land registry",
  "scripts": {
    "compile": "hardhat compile",
    "clean": "hardhat clean",
    "node": "hardhat node",
    "deploy": "hardhat run scripts/deploy.js --network localhost",
    "seed": "hardhat run scripts/seed.js --network localhost",
    "test": "hardhat test"
  },
  "devDependencies": {
    "@nomicfoundation/hardhat-toolbox": "^5.0.0",
    "dotenv": "^16.4.5",
    "hardhat": "^2.22.7"
  },
  "dependencies": {
    "land-registry-system": "file:.."
  }
}
